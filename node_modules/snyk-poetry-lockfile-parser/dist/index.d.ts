import { DepGraph } from '@snyk/dep-graph';
import * as manifest from './manifest-parser';
import * as lockFile from './lock-file-parser';
export declare function buildDepGraph(manifestFileContents: string, lockFileContents: string, includeDevDependencies?: boolean): DepGraph;
declare class DependencyNotFound extends Error {
    constructor(pkgName: string);
}
export declare type PoetryParsingError = manifest.ManifestFileNotValid | lockFile.LockFileNotValid | DependencyNotFound;
export {};
